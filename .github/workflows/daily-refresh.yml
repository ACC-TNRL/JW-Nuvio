name: Daily Refresh JustWatch NL

on:
  schedule:
    # Elke dag om 06:00 UTC (08:00 NL tijd)
    - cron: "0 6 * * *"
  workflow_dispatch: # Handmatig triggeren
  push:
    paths:
      - "scripts/scrape-jw.js" # Herdraaien als scraper verandert

jobs:
  refresh:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Scrape JustWatch & update cache
        env:
          JUSTWATCH_COUNTRY: ${{ vars.JUSTWATCH_COUNTRY || 'US' }}
          JUSTWATCH_LANGUAGE: ${{ vars.JUSTWATCH_LANGUAGE || 'en' }}
          CATALOG_LIMIT: ${{ vars.CATALOG_LIMIT || '50' }}
          ERDB_TOKEN: ${{ secrets.ERDB_TOKEN || '' }}
          ERDB_BASE_URL: ${{ vars.ERDB_BASE_URL || 'https://easyratingsdb.com' }}
        run: npm run refresh

      - name: Copy cache to repo root (for GitHub Pages)
        run: |
          cp cache/justwatch.us.trending_30_day.movies.json catalog/movie/justwatch.us.trending_30_day.movies.json
          cp cache/justwatch.us.trending_30_day.series.json catalog/series/justwatch.us.trending_30_day.series.json
          cp cache/justwatch.us.trending_7_day.movies.json catalog/movie/justwatch.us.trending_7_day.movies.json
          cp cache/justwatch.us.trending_7_day.series.json catalog/series/justwatch.us.trending_7_day.series.json

      - name: Commit & push cache
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add cache/*.json manifest.json catalog/
          if git diff --staged --quiet; then
            echo "No changes to cache"
          else
            git commit -m "chore: daily JustWatch NL cache refresh [skip ci]"
            git push
          fi
